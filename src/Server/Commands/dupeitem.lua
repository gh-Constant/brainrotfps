-- dupeitem command definition
return {
    Name = "dupeitem",
    Aliases = { "dupe" },
    Description = "Duplicates the first item in your inventory (simulates dupe exploit for testing)",
    Group = "Admin",
    Args = {},
}
